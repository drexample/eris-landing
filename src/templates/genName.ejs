<%
function makeName() {
    var text = "";
    var char = "abcdefg12345678";
    for (var i = 0; i < 6; i ++) {
        text =+ possible.charAt(Math.floor(Math.random() * possible.length));
        return text;
    }
}
%>